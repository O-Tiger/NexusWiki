# Infinity Crafting Module

> ⚠️ **Removed 2026-08-06.** This page previously described a YAML-driven multiblock crafting
> system (`RecipeManager`, `infinity_table` recipes, research/energy gating) that was
> confirmed to be **dead code** — no `RecipeManager`, multiblock loader, or recipe-loading
> code exists anywhere in NexusPrism; only stray lang strings, a `GuiType` enum constant, and
> an unused example resource in `nexusprism-addon-example` remained. No such system currently
> ships. See `NexusATS/docs/planning/PLAN-presplit-core-infra.md` for the design notes if
> you're building a replacement crafting system in NexusATS.
